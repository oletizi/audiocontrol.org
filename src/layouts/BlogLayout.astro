---
import Layout from './Layout.astro';

interface Heading {
  depth: number;
  slug: string;
  text: string;
}

interface Props {
  frontmatter?: {
    title: string;
    description?: string;
    date?: string;
    datePublished?: string;
    dateModified?: string;
    author?: string;
  };
  headings?: Heading[];
  title?: string;
  description?: string;
  date?: string;
  datePublished?: string;
  dateModified?: string;
  author?: string;
}

const { frontmatter, headings = [], title: directTitle, description: directDescription, date: directDate, datePublished: directDatePublished, dateModified: directDateModified, author: directAuthor } = Astro.props;
const title = frontmatter?.title ?? directTitle ?? 'Blog';
const description = frontmatter?.description ?? directDescription;
const date = frontmatter?.date ?? directDate;
const datePublished = frontmatter?.datePublished ?? directDatePublished;
const dateModified = frontmatter?.dateModified ?? directDateModified ?? datePublished;
const author = frontmatter?.author ?? directAuthor ?? 'audiocontrol.org';

const canonicalURL = new URL(Astro.url.pathname, Astro.site);

// Generate OG image path from URL: /blog/my-post/ → /images/og/blog-my-post.png
const pathSegments = Astro.url.pathname.split('/').filter(Boolean);
const ogImageSlug = pathSegments.join('-');
const ogImage = `/images/og/${ogImageSlug}.png`;

const blogPostingJsonLd = {
  '@context': 'https://schema.org',
  '@type': 'BlogPosting',
  headline: title,
  description: description,
  datePublished: datePublished,
  dateModified: dateModified,
  author: {
    '@type': 'Person',
    name: author,
  },
  publisher: {
    '@type': 'Organization',
    name: 'audiocontrol.org',
    logo: {
      '@type': 'ImageObject',
      url: 'https://audiocontrol.org/favicon.svg',
    },
  },
  mainEntityOfPage: {
    '@type': 'WebPage',
    '@id': canonicalURL.toString(),
  },
  image: 'https://audiocontrol.org/images/og-image.png',
};

const tocHeadings = headings.filter(h => h.depth === 2);
---

<Layout title={title} description={description} image={ogImage} type="article" jsonLd={blogPostingJsonLd}>
  <div class="article-layout">
    {tocHeadings.length > 0 && (
      <aside class="toc">
        <nav>
          <div class="toc-header">In this article</div>
          {tocHeadings.map(heading => (
            <a href={`#${heading.slug}`} class="toc-link">{heading.text}</a>
          ))}
        </nav>
      </aside>
    )}
    <article class="blog-article">
      {date && <p class="post-meta">{date}{author && ` · ${author}`}</p>}
      <slot />
    </article>
  </div>
</Layout>

<style>
  .article-layout {
    display: grid;
    grid-template-columns: 1fr;
    gap: 2rem;
  }

  @media (min-width: 1024px) {
    .article-layout {
      grid-template-columns: 200px 1fr;
    }
  }

  .toc {
    display: none;
  }

  @media (min-width: 1024px) {
    .toc {
      display: block;
      position: sticky;
      top: 5rem;
      align-self: start;
      max-height: calc(100vh - 6rem);
      overflow-y: auto;
    }
  }

  .toc-header {
    font-size: 0.85rem;
    font-weight: 500;
    color: var(--fg);
    margin-bottom: 1rem;
  }

  .toc-link {
    display: block;
    color: var(--muted);
    text-decoration: none;
    font-size: 0.85rem;
    line-height: 1.4;
    padding: 0.4rem 0 0.4rem 0.75rem;
    border-left: 2px solid transparent;
    transition: color 0.15s, border-color 0.15s;
  }

  .toc-link:hover,
  .toc-link.active {
    color: var(--fg);
    border-left-color: var(--accent);
  }

  .blog-article {
    line-height: 1.7;
    min-width: 0;
  }

  .post-meta {
    color: var(--muted);
    font-size: 0.9rem;
    margin-bottom: 2rem;
  }

  .blog-article :global(h1) {
    font-size: 2rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
    line-height: 1.2;
  }

  .blog-article :global(h2) {
    font-size: 1.5rem;
    font-weight: 600;
    margin-top: 2.5rem;
    margin-bottom: 1rem;
    color: var(--fg);
  }

  .blog-article :global(h3) {
    font-size: 1.25rem;
    font-weight: 600;
    margin-top: 2rem;
    margin-bottom: 0.75rem;
    color: var(--fg);
  }

  .blog-article :global(p) {
    margin-bottom: 1.25rem;
    color: var(--fg);
  }

  .blog-article :global(ul),
  .blog-article :global(ol) {
    margin-bottom: 1.25rem;
    padding-left: 1.5rem;
  }

  .blog-article :global(li) {
    margin-bottom: 0.5rem;
  }

  .blog-article :global(strong) {
    color: var(--fg);
    font-weight: 600;
  }

  .blog-article :global(a) {
    color: var(--accent);
    text-decoration: none;
  }

  .blog-article :global(a:hover) {
    text-decoration: underline;
  }

  .blog-article :global(hr) {
    border: none;
    border-top: 1px solid var(--border);
    margin: 2rem 0;
  }

  .blog-article :global(em) {
    color: var(--muted);
  }

  .blog-article :global(code) {
    background: var(--card-bg);
    padding: 0.2rem 0.4rem;
    border-radius: 4px;
    font-size: 0.9em;
  }

  .blog-article :global(pre) {
    background: var(--card-bg);
    padding: 1rem;
    border-radius: 8px;
    overflow-x: auto;
    margin-bottom: 1.25rem;
  }

  .blog-article :global(pre code) {
    background: none;
    padding: 0;
  }

  .blog-article :global(img) {
    max-width: 100%;
    height: auto;
    border-radius: 8px;
    margin: 1.5rem 0;
    border: 1px solid var(--border);
  }

  .blog-article :global(table) {
    width: 100%;
    border-collapse: separate;
    border-spacing: 0;
    margin: 1.5rem 0;
    border: 1px solid var(--border);
    border-radius: 8px;
  }

  .blog-article :global(th),
  .blog-article :global(td) {
    padding: 0.75rem 1rem;
    text-align: left;
    border-bottom: 1px solid var(--border);
  }

  .blog-article :global(th) {
    font-weight: 600;
    color: var(--fg);
    background: #323232;
  }

  .blog-article :global(tr:nth-child(even)) {
    background: #1e1e1e;
  }

  .blog-article :global(tr:hover) {
    background: rgba(74, 158, 255, 0.05);
  }

  .blog-article :global(tr:first-child th:first-child),
  .blog-article :global(tr:first-child td:first-child) {
    border-top-left-radius: 7px;
  }

  .blog-article :global(tr:first-child th:last-child),
  .blog-article :global(tr:first-child td:last-child) {
    border-top-right-radius: 7px;
  }

  .blog-article :global(tr:last-child td:first-child) {
    border-bottom-left-radius: 7px;
  }

  .blog-article :global(tr:last-child td:last-child) {
    border-bottom-right-radius: 7px;
  }

  .blog-article :global(figure) {
    margin: 1.5rem 0;
  }

  .blog-article :global(figure img) {
    margin: 0 0 0.5rem 0;
  }

  .blog-article :global(figcaption) {
    font-size: 0.9rem;
    color: var(--muted);
    font-style: italic;
  }
</style>

<script>
  const tocLinks = document.querySelectorAll('.toc-link');
  const headings = document.querySelectorAll('.blog-article h2');

  if (tocLinks.length > 0 && headings.length > 0) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            tocLinks.forEach((link) => link.classList.remove('active'));
            const activeLink = document.querySelector(`.toc-link[href="#${entry.target.id}"]`);
            if (activeLink) {
              activeLink.classList.add('active');
            }
          }
        });
      },
      {
        rootMargin: '-80px 0px -80% 0px',
        threshold: 0
      }
    );

    headings.forEach((heading) => observer.observe(heading));
  }
</script>
