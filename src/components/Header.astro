---
/**
 * Header component for the palette redesign
 * Features animated dot indicators, styled title, and navigation
 */
---

<header class="site-header">
  <a href="/" class="header-brand" aria-label="audiocontrol.org home">
    <div class="logo-dots" aria-hidden="true">
      <span class="dot dot-primary"></span>
      <span class="dot dot-accent"></span>
    </div>
    <div class="header-text">
      <h1 class="site-title">audiocontrol<span class="org">.org</span></h1>
      <p class="tagline">Open source audio tools.</p>
    </div>
  </a>

  <nav class="site-nav" aria-label="Main navigation">
    <a href="/blog/roland-s330-sampler-editor-feb-2026-update/" class="nav-link">Blog</a>
    <a href="/roland/s330/" class="nav-link">Hardware</a>
    <a href="/docs/roland/samplers/s-330/" class="nav-link">Docs</a>
    <a href="/roland/s330/editor" class="nav-link">Editors</a>
  </nav>
</header>

<style>
  .site-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    padding: 1.25rem 0;
    margin-bottom: 2rem;
    border-bottom: 1px solid hsl(var(--border));
  }

  .header-brand {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    text-decoration: none;
    color: inherit;
  }

  .header-brand:hover {
    text-decoration: none;
  }

  .header-brand:focus-visible {
    outline: 2px solid hsl(var(--primary));
    outline-offset: 4px;
    border-radius: 0.25rem;
  }

  .logo-dots {
    display: flex;
    align-items: center;
    gap: 0.375rem;
  }

  .dot {
    width: 0.5rem;
    height: 0.5rem;
    border-radius: 50%;
    animation: pulse-glow 2s ease-in-out infinite;
  }

  .dot-primary {
    background: hsl(var(--primary));
  }

  .dot-accent {
    background: hsl(var(--accent));
    animation-delay: 0.5s;
  }

  @keyframes pulse-glow {
    0%, 100% {
      opacity: 0.4;
    }
    50% {
      opacity: 1;
    }
  }

  .header-text {
    display: flex;
    flex-direction: column;
  }

  .site-title {
    font-family: "JetBrains Mono", monospace;
    font-size: 1.125rem;
    font-weight: 700;
    margin: 0;
    color: hsl(var(--foreground));
    letter-spacing: -0.025em;
  }

  .org {
    color: hsl(var(--primary));
  }

  .tagline {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.75rem;
    color: hsl(var(--muted-foreground));
    margin: 0;
  }

  .site-nav {
    display: flex;
    align-items: center;
    gap: 1.5rem;
  }

  .nav-link {
    font-family: "JetBrains Mono", monospace;
    font-size: 0.875rem;
    color: hsl(var(--muted-foreground));
    text-decoration: none;
    transition: color 0.2s ease;
  }

  .nav-link:hover {
    color: hsl(var(--foreground));
    text-decoration: none;
  }

  .nav-link:focus-visible {
    color: hsl(var(--foreground));
    outline: 2px solid hsl(var(--primary));
    outline-offset: 4px;
    border-radius: 0.125rem;
  }

  /* Mobile responsive */
  @media (max-width: 640px) {
    .site-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 1rem;
    }

    .site-nav {
      display: none;
    }
  }
</style>
