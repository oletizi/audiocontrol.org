---
import { getEntry } from 'astro:content';
import Layout from '../../../layouts/Layout.astro';
import {
  parsePageSlots,
  renderSlotBlock,
  renderSlotInline,
} from '../../../../../shared/page-slots';

interface CompatRow {
  label: string;
  value: string;
  status: 'confirmed' | 'likely' | 'unsupported' | 'note';
  note?: string;
}

const hardwareRows: CompatRow[] = [
  { label: 'Roland MC-500mkII', value: '1988', status: 'confirmed', note: 'Primary target. Built and tested against this unit.' },
  { label: 'Roland MC-50', value: '1989', status: 'likely', note: 'Smaller, cheaper sibling — same family. Untested. If you have one, file an issue with what you find.' },
  { label: 'Roland MC-500 (original)', value: '1986', status: 'unsupported', note: 'Different operating system, different transport behavior. Does not work.' },
];

const dawRows: CompatRow[] = [
  { label: 'Logic Pro', value: 'macOS', status: 'confirmed' },
  { label: 'Ableton Live', value: 'macOS', status: 'confirmed' },
  { label: 'Universal Audio LUNA', value: 'macOS', status: 'confirmed' },
  { label: 'Other DAWs', value: 'Mackie Control', status: 'note', note: 'If your DAW takes a hardware control surface for transport, this is one. If it does not, this is not.' },
];

const platformRows: CompatRow[] = [
  { label: 'macOS', value: 'Universal binary', status: 'confirmed' },
  { label: 'Linux', value: 'Builds; untested', status: 'note', note: 'The Linux build runs but has not been exercised against a DAW.' },
  { label: 'Windows', value: 'Not yet', status: 'unsupported' },
];

const downloadHref = 'https://github.com/audiocontrol-org/audiocontrol/releases?q=midi-macro-bridge';
const sourceHref = 'https://github.com/audiocontrol-org/audiocontrol';
const dispatchHref = '/blog/midi-to-mcu-macro-bridge/';

const entry = await getEntry('pages', 'bridges/midi-macro-bridge');
if (!entry) {
  throw new Error('Missing content entry: pages/bridges/midi-macro-bridge. Expected src/sites/audiocontrol/content/pages/bridges/midi-macro-bridge/index.md');
}
const slots = parsePageSlots(entry.body ?? '');

const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'SoftwareApplication',
  name: 'MIDI Macro Bridge',
  applicationCategory: 'MultimediaApplication',
  applicationSubCategory: 'DAW Control Surface Bridge',
  operatingSystem: 'macOS',
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'USD' },
  url: 'https://audiocontrol.org/bridges/midi-macro-bridge/',
  downloadUrl: downloadHref,
  description: entry.data.description,
  publisher: { '@type': 'Organization', name: 'audiocontrol.org', url: 'https://audiocontrol.org' },
  isAccessibleForFree: true,
  license: 'https://opensource.org/licenses/MIT',
};
---

<Layout
  title={slots.raw.metaTitle ?? 'MIDI Macro Bridge'}
  description={entry.data.description}
  image="/images/bridges/control-surface.png"
  type="website"
  jsonLd={jsonLd}
>
  <article class="product">
    <header class="hero" aria-labelledby="product-title">
      <div class="hero-content">
        <p class="hero-eyebrow">
          <span class="signal-led" aria-hidden="true"></span>
          <span set:html={renderSlotInline(slots, 'heroKind')} />
          <span class="hero-sep" aria-hidden="true">·</span>
          <span set:html={renderSlotInline(slots, 'heroVersion')} />
        </p>
        <h1 id="product-title" class="hero-title">
          <span set:html={renderSlotInline(slots, 'heroTitle')} /><br />
          <span class="phosphor" set:html={renderSlotInline(slots, 'heroPhosphor')} />
        </h1>
        <div class="hero-body" set:html={renderSlotBlock(slots, 'heroBody')} />

        <div class="hero-actions">
          <a class="btn btn-primary" href={downloadHref} rel="noopener">
            <span class="btn-arrow" aria-hidden="true">↓</span>
            <span set:html={renderSlotInline(slots, 'ctaDownload')} />
          </a>
          <a class="btn btn-ghost" href={dispatchHref}>
            <span class="btn-arrow" aria-hidden="true">→</span>
            <span set:html={renderSlotInline(slots, 'ctaDispatch')} />
          </a>
          <a class="btn btn-ghost" href={sourceHref} rel="noopener">
            <span class="btn-arrow" aria-hidden="true">↳</span>
            <span set:html={renderSlotInline(slots, 'ctaSource')} />
          </a>
        </div>

        <hr class="rule-ticked" />

        <dl class="hero-stats" aria-label="Bridge specifications">
          <div>
            <dt set:html={renderSlotInline(slots, 'statHardwareLabel')} />
            <dd set:html={renderSlotInline(slots, 'statHardwareValue')} />
          </div>
          <div>
            <dt set:html={renderSlotInline(slots, 'statDawsLabel')} />
            <dd set:html={renderSlotInline(slots, 'statDawsValue')} />
          </div>
          <div>
            <dt set:html={renderSlotInline(slots, 'statProtocolLabel')} />
            <dd set:html={renderSlotInline(slots, 'statProtocolValue')} />
          </div>
          <div>
            <dt set:html={renderSlotInline(slots, 'statCostLabel')} />
            <dd set:html={renderSlotInline(slots, 'statCostValue')} />
          </div>
        </dl>
      </div>

      <aside class="hero-aside">
        <figure class="screen-frame">
          <header class="screen-frame-head">
            <span class="screen-frame-led" aria-hidden="true"></span>
            <span class="screen-frame-fig" set:html={renderSlotInline(slots, 'heroPhotoLabel')} />
            <span class="screen-frame-sep" aria-hidden="true">·</span>
            <span class="screen-frame-subject" set:html={renderSlotInline(slots, 'heroPhotoSubject')} />
          </header>
          <div class="screen-frame-body">
            <img
              class="hero-photo"
              src="/images/bridges/mc-500.jpg"
              alt={slots.raw.heroFigureAlt ?? ''}
              loading="eager"
              decoding="async"
            />
            <div class="screen-frame-overlay" aria-hidden="true"></div>
            <span class="screen-frame-corner screen-frame-corner--tl" aria-hidden="true"></span>
            <span class="screen-frame-corner screen-frame-corner--tr" aria-hidden="true"></span>
            <span class="screen-frame-corner screen-frame-corner--bl" aria-hidden="true"></span>
            <span class="screen-frame-corner screen-frame-corner--br" aria-hidden="true"></span>
          </div>
          <figcaption class="screen-frame-foot" set:html={renderSlotInline(slots, 'heroPhotoMeta')} />
        </figure>
      </aside>
    </header>

    <section class="block" aria-labelledby="bench-label">
      <header class="block-header">
        <h2 id="bench-label" class="panel-label" set:html={renderSlotInline(slots, 'benchLabel')} />
        {slots.raw.benchNote && (
          <div class="block-note" set:html={renderSlotBlock(slots, 'benchNote')} />
        )}
      </header>

      <div class="demo-pair">
        <figure class="demo-frame">
          <header class="demo-frame-head">
            <span class="demo-frame-led" aria-hidden="true"></span>
            <span class="demo-frame-fig" set:html={renderSlotInline(slots, 'videoLabel')} />
            <span class="demo-frame-sep" aria-hidden="true">·</span>
            <span class="demo-frame-subject" set:html={renderSlotInline(slots, 'videoSubject')} />
          </header>
          {slots.raw.videoBlurb && (
            <div class="demo-frame-blurb" set:html={renderSlotBlock(slots, 'videoBlurb')} />
          )}
          <div class="demo-frame-body demo-frame-body--video">
            <iframe
              class="demo-frame-iframe"
              src="https://www.youtube.com/embed/IIhBlliWr9k"
              title={slots.raw.videoTitle ?? 'Demo video'}
              allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
              allowfullscreen
              loading="lazy"
            ></iframe>
            <span class="demo-frame-corner demo-frame-corner--tl" aria-hidden="true"></span>
            <span class="demo-frame-corner demo-frame-corner--tr" aria-hidden="true"></span>
            <span class="demo-frame-corner demo-frame-corner--bl" aria-hidden="true"></span>
            <span class="demo-frame-corner demo-frame-corner--br" aria-hidden="true"></span>
          </div>
          <figcaption class="demo-frame-foot" set:html={renderSlotInline(slots, 'videoCaption')} />
        </figure>

        <figure class="demo-frame">
          <header class="demo-frame-head">
            <span class="demo-frame-led" aria-hidden="true"></span>
            <span class="demo-frame-fig" set:html={renderSlotInline(slots, 'figureLabel')} />
            <span class="demo-frame-sep" aria-hidden="true">·</span>
            <span class="demo-frame-subject" set:html={renderSlotInline(slots, 'figureSubject')} />
          </header>
          {slots.raw.figureBlurb && (
            <div class="demo-frame-blurb" set:html={renderSlotBlock(slots, 'figureBlurb')} />
          )}
          <div class="demo-frame-body demo-frame-body--still">
            <img
              class="demo-frame-image"
              src="/images/bridges/control-surface.png"
              alt={slots.raw.figureAlt ?? ''}
              loading="lazy"
            />
            <span class="demo-frame-corner demo-frame-corner--tl" aria-hidden="true"></span>
            <span class="demo-frame-corner demo-frame-corner--tr" aria-hidden="true"></span>
            <span class="demo-frame-corner demo-frame-corner--bl" aria-hidden="true"></span>
            <span class="demo-frame-corner demo-frame-corner--br" aria-hidden="true"></span>
          </div>
          <figcaption class="demo-frame-foot" set:html={renderSlotInline(slots, 'figureCaption')} />
        </figure>
      </div>
    </section>

    <section class="block" aria-labelledby="what-it-does-label">
      <header class="block-header">
        <h2 id="what-it-does-label" class="panel-label" set:html={renderSlotInline(slots, 'capabilitiesLabel')} />
      </header>
      <div class="capabilities">
        <div class="capability">
          <p class="capability-tag" set:html={renderSlotInline(slots, 'capability1Tag')} />
          <h3 class="capability-title" set:html={renderSlotInline(slots, 'capability1Title')} />
          <div class="capability-body" set:html={renderSlotBlock(slots, 'capability1Body')} />
        </div>
        <div class="capability">
          <p class="capability-tag" set:html={renderSlotInline(slots, 'capability2Tag')} />
          <h3 class="capability-title" set:html={renderSlotInline(slots, 'capability2Title')} />
          <div class="capability-body" set:html={renderSlotBlock(slots, 'capability2Body')} />
        </div>
        <div class="capability">
          <p class="capability-tag" set:html={renderSlotInline(slots, 'capability3Tag')} />
          <h3 class="capability-title" set:html={renderSlotInline(slots, 'capability3Title')} />
          <div class="capability-body" set:html={renderSlotBlock(slots, 'capability3Body')} />
        </div>
      </div>
    </section>

    <section class="block" aria-labelledby="compat-label">
      <header class="block-header">
        <h2 id="compat-label" class="panel-label panel-label--accent" set:html={renderSlotInline(slots, 'compatLabel')} />
        {slots.raw.compatNote && (
          <div class="block-note" set:html={renderSlotBlock(slots, 'compatNote')} />
        )}
      </header>

      <div class="compat-group">
        <p class="compat-group-label">Hardware</p>
        <ul class="compat-list">
          {hardwareRows.map((row) => (
            <li class={`compat-row compat-row--${row.status}`}>
              <span class="compat-status" aria-hidden="true">
                {row.status === 'confirmed' && '✓'}
                {row.status === 'likely' && '?'}
                {row.status === 'unsupported' && '✕'}
                {row.status === 'note' && '·'}
              </span>
              <span class="compat-label">{row.label}</span>
              <span class="compat-value">{row.value}</span>
              {row.note && <span class="compat-note">{row.note}</span>}
            </li>
          ))}
        </ul>
      </div>

      <div class="compat-group">
        <p class="compat-group-label">DAWs</p>
        <ul class="compat-list">
          {dawRows.map((row) => (
            <li class={`compat-row compat-row--${row.status}`}>
              <span class="compat-status" aria-hidden="true">
                {row.status === 'confirmed' && '✓'}
                {row.status === 'likely' && '?'}
                {row.status === 'unsupported' && '✕'}
                {row.status === 'note' && '·'}
              </span>
              <span class="compat-label">{row.label}</span>
              <span class="compat-value">{row.value}</span>
              {row.note && <span class="compat-note">{row.note}</span>}
            </li>
          ))}
        </ul>
      </div>

      <div class="compat-group">
        <p class="compat-group-label">Platform</p>
        <ul class="compat-list">
          {platformRows.map((row) => (
            <li class={`compat-row compat-row--${row.status}`}>
              <span class="compat-status" aria-hidden="true">
                {row.status === 'confirmed' && '✓'}
                {row.status === 'likely' && '?'}
                {row.status === 'unsupported' && '✕'}
                {row.status === 'note' && '·'}
              </span>
              <span class="compat-label">{row.label}</span>
              <span class="compat-value">{row.value}</span>
              {row.note && <span class="compat-note">{row.note}</span>}
            </li>
          ))}
        </ul>
      </div>
    </section>

    <section class="block install" aria-labelledby="install-label">
      <header class="block-header">
        <h2 id="install-label" class="panel-label" set:html={renderSlotInline(slots, 'installLabel')} />
      </header>
      <ol class="install-steps">
        <li>
          <span class="install-step-num" aria-hidden="true">01</span>
          <div class="install-step-body" set:html={renderSlotBlock(slots, 'installStep1')} />
        </li>
        <li>
          <span class="install-step-num" aria-hidden="true">02</span>
          <div class="install-step-body" set:html={renderSlotBlock(slots, 'installStep2')} />
        </li>
        <li>
          <span class="install-step-num" aria-hidden="true">03</span>
          <div class="install-step-body" set:html={renderSlotBlock(slots, 'installStep3')} />
        </li>
      </ol>

      <div class="install-cta">
        <a class="btn btn-primary" href={downloadHref} rel="noopener">
          <span class="btn-arrow" aria-hidden="true">↓</span>
          <span set:html={renderSlotInline(slots, 'installCta')} />
        </a>
      </div>
    </section>

    <section class="block roadmap" aria-labelledby="roadmap-label">
      <header class="block-header">
        <h2 id="roadmap-label" class="panel-label" set:html={renderSlotInline(slots, 'roadmapLabel')} />
        {slots.raw.roadmapNote && (
          <div class="block-note" set:html={renderSlotBlock(slots, 'roadmapNote')} />
        )}
      </header>
      <ul class="roadmap-list">
        <li>
          <span class="roadmap-tag" set:html={renderSlotInline(slots, 'roadmap1Tag')} />
          <div class="roadmap-body" set:html={renderSlotBlock(slots, 'roadmap1Body')} />
        </li>
        <li>
          <span class="roadmap-tag" set:html={renderSlotInline(slots, 'roadmap2Tag')} />
          <div class="roadmap-body" set:html={renderSlotBlock(slots, 'roadmap2Body')} />
        </li>
      </ul>
    </section>

    <section class="block report" aria-labelledby="report-label">
      <header class="block-header">
        <h2 id="report-label" class="panel-label" set:html={renderSlotInline(slots, 'reportLabel')} />
      </header>
      <div class="report-body" set:html={renderSlotBlock(slots, 'reportBody')} />
    </section>
  </article>
</Layout>

<style>
  .product {
    padding-bottom: 2rem;
  }

  /* --- Hero --------------------------------------------------------- */

  .hero {
    padding: 3rem 0 2.5rem;
    display: grid;
    grid-template-columns: minmax(0, 1.15fr) minmax(0, 1fr);
    gap: clamp(1.5rem, 3vw, 3rem);
    align-items: start;
  }

  .hero-content {
    max-width: 38rem;
  }

  .hero-aside {
    align-self: stretch;
    display: flex;
    min-height: 0;
  }

  /* --- Screen frame (CRT-overlay photo) --------------------------- */

  .screen-frame {
    flex: 1;
    margin: 0;
    display: flex;
    flex-direction: column;
    background: hsl(var(--card));
    border: 1px solid hsl(var(--border));
    box-shadow:
      0 0 0 1px hsl(var(--background)) inset,
      0 18px 38px -22px rgba(0, 0, 0, 0.75);
  }

  .screen-frame-head,
  .screen-frame-foot {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.7rem 1rem;
    font-family: var(--font-display);
    font-size: 0.66rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: hsl(var(--muted-foreground));
  }

  .screen-frame-head {
    border-bottom: 1px dashed hsl(var(--border));
  }

  .screen-frame-foot {
    border-top: 1px dashed hsl(var(--border));
    margin: 0;
  }

  .screen-frame-led {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: hsl(var(--primary));
    box-shadow: 0 0 9px hsl(var(--primary) / 0.65);
    flex-shrink: 0;
  }

  .screen-frame-fig {
    color: hsl(var(--primary));
    letter-spacing: 0.22em;
    text-shadow: 0 0 10px hsl(var(--primary) / 0.35);
  }

  .screen-frame-sep {
    opacity: 0.5;
  }

  .screen-frame-subject {
    color: hsl(var(--foreground));
    letter-spacing: 0.14em;
  }

  .screen-frame-body {
    position: relative;
    flex: 1;
    min-height: 320px;
    overflow: hidden;
    background: #0a0a0a;
  }

  .hero-photo {
    display: block;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 40%;
    /* Cool CRT grade: very slight desaturation, faint cool push */
    filter:
      contrast(1.03)
      saturate(0.95)
      brightness(0.97)
      hue-rotate(-2deg);
  }

  /* CRT overlay — scanlines + radial vignette. Tuned to read as
     ambient atmosphere rather than literal scanlines. */
  .screen-frame-overlay {
    position: absolute;
    inset: 0;
    pointer-events: none;
    background:
      /* fine scanlines: 1px line, 5px gap, low opacity */
      repeating-linear-gradient(
        to bottom,
        rgba(0, 0, 0, 0.22) 0,
        rgba(0, 0, 0, 0.22) 1px,
        transparent 1px,
        transparent 6px
      ),
      /* gentle radial vignette */
      radial-gradient(
        ellipse 80% 75% at 50% 48%,
        transparent 45%,
        rgba(0, 0, 0, 0.10) 75%,
        rgba(0, 0, 0, 0.32) 100%
      );
    mix-blend-mode: multiply;
  }

  /* Faint chartreuse phosphor bloom along the bottom edge — the
     "tube glow" that peeks from below the visible image area. */
  .screen-frame-body::after {
    content: '';
    position: absolute;
    inset: 0;
    pointer-events: none;
    background: linear-gradient(
      to bottom,
      transparent 82%,
      hsl(var(--primary) / 0.05) 94%,
      hsl(var(--primary) / 0.09) 100%
    );
  }

  /* Service-manual corner brackets — same vocabulary as the project
     cards, just sized smaller and inset onto the photo. */
  .screen-frame-corner {
    position: absolute;
    width: 11px;
    height: 11px;
    border-color: hsl(var(--primary) / 0.55);
    border-style: solid;
    border-width: 0;
    pointer-events: none;
  }
  .screen-frame-corner--tl { top: 8px; left: 8px; border-top-width: 1px; border-left-width: 1px; }
  .screen-frame-corner--tr { top: 8px; right: 8px; border-top-width: 1px; border-right-width: 1px; }
  .screen-frame-corner--bl { bottom: 8px; left: 8px; border-bottom-width: 1px; border-left-width: 1px; }
  .screen-frame-corner--br { bottom: 8px; right: 8px; border-bottom-width: 1px; border-right-width: 1px; }

  @media (max-width: 900px) {
    .hero {
      grid-template-columns: 1fr;
    }
    .hero-aside {
      order: -1;
      max-width: 32rem;
    }
    .screen-frame-body {
      min-height: 240px;
      max-height: 380px;
    }
  }

  .hero-eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-family: var(--font-display);
    font-size: 0.68rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: hsl(var(--muted-foreground));
    margin: 0 0 1.5rem 0;
  }

  .hero-eyebrow .signal-led {
    width: 7px;
    height: 7px;
  }

  .hero-sep {
    opacity: 0.5;
  }

  .hero-title {
    font-family: var(--font-display);
    font-size: clamp(1.85rem, 4vw, 3rem);
    font-weight: 400;
    line-height: 1.15;
    margin: 0;
    color: hsl(var(--foreground));
  }

  .hero-title .phosphor {
    color: hsl(var(--primary));
    text-shadow: 0 0 22px hsl(var(--primary) / 0.35);
  }

  .hero-body {
    font-size: 1.05rem;
    line-height: 1.65;
    color: hsl(var(--muted-foreground));
    max-width: 42rem;
    margin: 1.5rem 0 2rem;
  }

  .hero-body :global(p) {
    margin: 0 0 1em 0;
  }

  .hero-body :global(p:last-child) {
    margin-bottom: 0;
  }

  .hero-actions {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
    margin: 0 0 2.5rem 0;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    font-family: var(--font-display);
    font-size: 0.82rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    padding: 0.8rem 1.25rem;
    border: 1px solid hsl(var(--border));
    text-decoration: none;
    color: inherit;
    transition: border-color 0.2s ease, background 0.2s ease, color 0.2s ease, transform 0.2s ease;
  }

  .btn:hover {
    text-decoration: none;
    transform: translateY(-1px);
  }

  .btn-arrow {
    color: hsl(var(--primary));
    transition: transform 0.2s ease;
  }

  .btn-primary {
    background: hsl(var(--primary) / 0.08);
    border-color: hsl(var(--primary) / 0.5);
    color: hsl(var(--foreground));
  }

  .btn-primary:hover {
    background: hsl(var(--primary) / 0.14);
    border-color: hsl(var(--primary));
    box-shadow: 0 0 24px hsl(var(--primary) / 0.15);
  }

  .btn-primary:hover .btn-arrow {
    transform: translateY(2px);
  }

  .btn-ghost {
    background: transparent;
    color: hsl(var(--muted-foreground));
  }

  .btn-ghost:hover {
    background: hsl(var(--card));
    color: hsl(var(--foreground));
    border-color: hsl(var(--border-hover));
  }

  .btn-ghost:hover .btn-arrow {
    transform: translateX(3px);
  }

  .btn:focus-visible {
    outline: 2px solid hsl(var(--primary));
    outline-offset: 3px;
  }

  .hero-stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem 2rem;
    margin: 1.5rem 0 0 0;
  }

  .hero-stats > div {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }

  .hero-stats dt {
    font-family: var(--font-display);
    font-size: 0.64rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: hsl(var(--muted-foreground));
  }

  .hero-stats dd {
    font-family: var(--font-display);
    font-size: 0.95rem;
    color: hsl(var(--foreground));
    margin: 0;
  }

  @media (max-width: 720px) {
    .hero {
      padding: 2rem 0 1.5rem;
    }
    .hero-stats {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* --- Demo pair (video + runtime screenshot) --------------------- */

  .demo-pair {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: clamp(1rem, 2.4vw, 1.75rem);
    margin: 0 0 2.5rem 0;
  }

  .demo-frame {
    margin: 0;
    display: flex;
    flex-direction: column;
    background: hsl(var(--card));
    border: 1px solid hsl(var(--border));
    box-shadow:
      0 0 0 1px hsl(var(--background)) inset,
      0 16px 32px -22px rgba(0, 0, 0, 0.7);
  }

  .demo-frame-head,
  .demo-frame-foot {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    padding: 0.7rem 1rem;
    font-family: var(--font-display);
    font-size: 0.66rem;
    letter-spacing: 0.18em;
    text-transform: uppercase;
    color: hsl(var(--muted-foreground));
  }

  .demo-frame-head {
    border-bottom: 1px dashed hsl(var(--border));
  }

  .demo-frame-foot {
    border-top: 1px dashed hsl(var(--border));
    margin: 0;
  }

  .demo-frame-led {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: hsl(var(--primary));
    box-shadow: 0 0 9px hsl(var(--primary) / 0.65);
    flex-shrink: 0;
  }

  .demo-frame-fig {
    color: hsl(var(--primary));
    letter-spacing: 0.22em;
    text-shadow: 0 0 10px hsl(var(--primary) / 0.35);
  }

  .demo-frame-sep {
    opacity: 0.5;
  }

  .demo-frame-subject {
    color: hsl(var(--foreground));
    letter-spacing: 0.14em;
  }

  /* Editorial blurb between FIG header and media. Uses body type (not
     monospace display) so it reads as figure-caption prose, not as
     telemetry. Sits flush against the dashed register above. */
  .demo-frame-blurb {
    padding: 0.95rem 1.1rem 1.05rem;
    font-size: 0.92rem;
    line-height: 1.55;
    color: hsl(var(--muted-foreground));
    background:
      linear-gradient(
        to right,
        hsl(var(--primary) / 0.04),
        transparent 40%
      );
    border-left: 2px solid hsl(var(--primary) / 0.55);
  }

  .demo-frame-blurb :global(p) {
    margin: 0;
  }

  .demo-frame-blurb :global(p) + :global(p) {
    margin-top: 0.55em;
  }

  .demo-frame-body {
    position: relative;
    aspect-ratio: 16 / 9;
    overflow: hidden;
    background: #000;
  }

  .demo-frame-iframe {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    border: 0;
    display: block;
  }

  .demo-frame-image {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: contain;
    object-position: center;
    background: #000;
    display: block;
  }

  /* Match the hero's corner-bracket vocabulary so the page reads as
     one register. Inset onto the media area, chartreuse, low alpha. */
  .demo-frame-corner {
    position: absolute;
    width: 11px;
    height: 11px;
    border-color: hsl(var(--primary) / 0.55);
    border-style: solid;
    border-width: 0;
    pointer-events: none;
    z-index: 1;
  }
  .demo-frame-corner--tl { top: 8px; left: 8px; border-top-width: 1px; border-left-width: 1px; }
  .demo-frame-corner--tr { top: 8px; right: 8px; border-top-width: 1px; border-right-width: 1px; }
  .demo-frame-corner--bl { bottom: 8px; left: 8px; border-bottom-width: 1px; border-left-width: 1px; }
  .demo-frame-corner--br { bottom: 8px; right: 8px; border-bottom-width: 1px; border-right-width: 1px; }

  @media (max-width: 900px) {
    .demo-pair {
      grid-template-columns: 1fr;
    }
  }

  /* --- Block scaffolding ------------------------------------------- */

  .block {
    padding: 2rem 0;
  }

  .block-header {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 1rem;
    margin: 0 0 1.25rem 0;
    flex-wrap: wrap;
  }

  .block-header .panel-label {
    margin: 0;
  }

  .block-note {
    font-size: 0.9rem;
    color: hsl(var(--muted-foreground));
    margin: 0;
  }

  .block-note :global(p) {
    margin: 0;
  }

  /* --- Capabilities ------------------------------------------------- */

  .capabilities {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  @media (max-width: 900px) {
    .capabilities {
      grid-template-columns: 1fr;
    }
  }

  .capability {
    padding: 1.25rem;
    background: hsl(var(--card));
    border: 1px solid hsl(var(--border));
  }

  .capability-tag {
    font-family: var(--font-display);
    font-size: 0.66rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: hsl(var(--primary));
    margin: 0 0 0.65rem 0;
  }

  .capability-title {
    font-family: var(--font-body);
    font-weight: 600;
    font-size: 1.02rem;
    color: hsl(var(--foreground));
    margin: 0 0 0.5rem 0;
    line-height: 1.35;
  }

  .capability-body {
    font-size: 0.9rem;
    color: hsl(var(--muted-foreground));
    line-height: 1.55;
    margin: 0;
  }

  .capability-body :global(p) {
    margin: 0;
  }

  /* --- Compatibility ----------------------------------------------- */

  .compat-group + .compat-group {
    margin-top: 1.5rem;
  }

  .compat-group-label {
    font-family: var(--font-display);
    font-size: 0.66rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: hsl(var(--muted-foreground));
    margin: 0 0 0.6rem 0;
  }

  .compat-list {
    list-style: none;
    padding: 0;
    margin: 0;
    border-top: 1px solid hsl(var(--border));
  }

  .compat-row {
    display: grid;
    grid-template-columns: 1.75rem minmax(11rem, 18rem) minmax(8rem, 14rem) 1fr;
    align-items: baseline;
    gap: 0.85rem;
    padding: 0.7rem 0;
    border-bottom: 1px solid hsl(var(--border));
  }

  .compat-status {
    font-family: var(--font-display);
    font-size: 0.95rem;
    text-align: center;
  }

  .compat-row--confirmed .compat-status {
    color: hsl(var(--primary));
    text-shadow: 0 0 8px hsl(var(--primary) / 0.4);
  }

  .compat-row--likely .compat-status {
    color: hsl(var(--accent));
  }

  .compat-row--unsupported .compat-status {
    color: hsl(var(--muted-foreground));
    opacity: 0.7;
  }

  .compat-row--note .compat-status {
    color: hsl(var(--muted-foreground));
    opacity: 0.55;
  }

  .compat-row--unsupported {
    opacity: 0.65;
  }

  .compat-label {
    font-family: var(--font-body);
    font-weight: 600;
    color: hsl(var(--foreground));
    font-size: 0.95rem;
  }

  .compat-value {
    font-family: var(--font-mono);
    font-size: 0.78rem;
    letter-spacing: 0.04em;
    color: hsl(var(--muted-foreground));
    text-transform: uppercase;
  }

  .compat-note {
    font-size: 0.88rem;
    color: hsl(var(--muted-foreground));
    line-height: 1.5;
  }

  @media (max-width: 720px) {
    .compat-row {
      grid-template-columns: 1.5rem 1fr;
      grid-template-rows: auto auto auto;
      row-gap: 0.25rem;
    }
    .compat-status {
      grid-row: 1 / 4;
    }
    .compat-value {
      grid-column: 2;
    }
    .compat-note {
      grid-column: 2;
    }
  }

  /* --- Install ----------------------------------------------------- */

  .install-steps {
    list-style: none;
    padding: 0;
    margin: 0;
    counter-reset: step;
  }

  .install-steps li {
    display: grid;
    grid-template-columns: 3.25rem 1fr;
    gap: 1rem;
    align-items: baseline;
    padding: 1rem 0;
    border-bottom: 1px solid hsl(var(--border));
  }

  .install-steps li:first-child {
    border-top: 1px solid hsl(var(--border));
  }

  .install-step-num {
    font-family: var(--font-display);
    font-size: 0.95rem;
    color: hsl(var(--primary));
    letter-spacing: 0.05em;
  }

  .install-step-body :global(p) {
    margin: 0;
    font-size: 0.98rem;
    line-height: 1.6;
    color: hsl(var(--foreground));
  }

  .install-step-body :global(code) {
    font-family: var(--font-mono);
    font-size: 0.85em;
    background: hsl(var(--card));
    border: 1px solid hsl(var(--border));
    padding: 0.05rem 0.35rem;
    color: hsl(var(--primary));
  }

  .install-step-body :global(a) {
    color: hsl(var(--primary));
    text-decoration: underline;
    text-decoration-color: hsl(var(--primary) / 0.45);
    text-underline-offset: 2px;
  }

  .install-cta {
    margin: 1.5rem 0 0 0;
  }

  /* --- Roadmap ----------------------------------------------------- */

  .roadmap-list {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .roadmap-list li {
    display: grid;
    grid-template-columns: 5rem 1fr;
    gap: 1rem;
    align-items: baseline;
    padding: 0.85rem 0;
    border-bottom: 1px solid hsl(var(--border));
  }

  .roadmap-list li:first-child {
    border-top: 1px solid hsl(var(--border));
  }

  .roadmap-tag {
    font-family: var(--font-display);
    font-size: 0.66rem;
    letter-spacing: 0.16em;
    text-transform: uppercase;
    color: hsl(var(--primary));
  }

  .roadmap-body :global(p) {
    margin: 0;
    font-size: 0.95rem;
    line-height: 1.55;
    color: hsl(var(--muted-foreground));
  }

  .roadmap-body :global(strong) {
    color: hsl(var(--foreground));
    font-weight: 600;
  }

  /* --- Report ------------------------------------------------------ */

  .report-body {
    max-width: 46rem;
    font-size: 0.98rem;
    line-height: 1.65;
    color: hsl(var(--muted-foreground));
    margin: 0;
  }

  .report-body :global(p) {
    margin: 0;
  }

  .report-body :global(a) {
    color: hsl(var(--primary));
    text-decoration: underline;
    text-decoration-color: hsl(var(--primary) / 0.45);
    text-underline-offset: 2px;
  }
</style>
