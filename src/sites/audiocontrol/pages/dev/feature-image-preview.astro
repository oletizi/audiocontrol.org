---
/*
 * /dev/feature-image-preview — redirect stub.
 *
 * The single-page Feature Image Studio moved to /dev/studio in
 * Phase 16. The old 4096-line implementation is preserved in git
 * history at a1a6615 and earlier (pre-redesign); this stub exists
 * so bookmarks and muscle memory keep working.
 */

export const prerender = false;

if (import.meta.env.PROD) {
  return new Response('Not Found', { status: 404 });
}

return Astro.redirect('/dev/studio', 302);
---
