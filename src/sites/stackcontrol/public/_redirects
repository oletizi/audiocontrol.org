# stackcontrol.org redirects.
# Parallel to the sibling sites' public/_redirects — per-site redirects only
# apply to this site's build output.

# Sitemap alias so external linkers hitting /sitemap.xml find the real one.
/sitemap.xml  /sitemap-index.xml  301
