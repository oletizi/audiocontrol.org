# build output
dist/
.netlify/
# generated types
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/
test-results/

# Playwright MCP scratch output (screenshots, snapshots, console logs)
.playwright-mcp/

# scratch output from feature-image CLI iterations (path follows publicDir split)
src/sites/audiocontrol/public/images/generated/

# (note: .feature-image-history.jsonl and .feature-image-pipeline.jsonl
# are intentionally NOT gitignored — they hold the generation history
# and workflow-decision audit trail that the Phase 11 prompt library
# will compute fitness scores from. Lose them = lose the design-system
# evolution.)
